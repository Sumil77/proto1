spring:
  application:
    name: proto1
  datasource:
    url: jdbc:postgresql://localhost:5432/protodb1
    username: postgres
    password: Mannkibaat@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always